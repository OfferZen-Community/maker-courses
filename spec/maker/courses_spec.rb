require "spec_helper"

RSpec.describe Maker::Courses do
  it "has a version number" do
    expect(Maker::Courses::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
